{
  "scripts": {
    "test": "cypress open",
    "report-chrome": "cypress run --browser chrome --reporter cypress-multi-reporters --reporter-options configFile=config-report-chrome.json --spec",
    "report-edge": "cypress run --browser edge --reporter cypress-multi-reporters --reporter-options configFile=config-report-edge.json --spec"
  },
  "devDependencies": {
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.3",
    "@types/node": "^22.6.1",
    "cypress": "^13.14.2",
    "cypress-multi-reporters": "^1.6.4",
    "cypress-plugin-api": "^2.11.2",
    "cypress-plugin-steps": "^1.1.1",
    "cypress-real-events": "^1.13.0",
    "esbuild": "^0.24.0",
    "mocha": "^10.7.3",
    "mocha-junit-reporter": "^2.2.1",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "typescript": "^5.6.2"
  }
}
